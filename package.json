{
  "name": "ofp-companion",
  "version": "1.8.5",
  "private": true,
  "description": "Offline OFP companion and Journey Log — the app itself ships no dependencies; these scripts run its tests.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "smoke": "node tests/smoke/run.mjs"
  },
  "license": "MIT"
}
